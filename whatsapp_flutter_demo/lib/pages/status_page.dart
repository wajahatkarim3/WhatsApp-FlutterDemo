import 'package:flutter/material.dart';

class StatusPagea extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return new Container(
      child: new Center(
        child: new Text('Status Page', style: new TextStyle(fontSize: 20.0))
      )
    );
  }
}
